import { DashboardOverviewView } from "@/components/pages/admin/dashboard-overview-view";

export const metadata = {
  title: "Admin Dashboard | Smart Library",
  description: "Executive library telemetry and active seat reservations",
};

export default function AdminOverviewPage() {
  return <DashboardOverviewView />;
}
